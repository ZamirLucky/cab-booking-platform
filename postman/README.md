# postman/

Postman collection for testing the Cab Booking Platform API.

Import `cab-booking-platform.postman_collection.placeholder.json` into Postman.

TODO: Replace placeholder file with a real exported Postman collection once routes are implemented.

Suggested environment variables in Postman:
- `GATEWAY_URL` — e.g. http://localhost:3000
- `TOKEN` — JWT returned from the login request
